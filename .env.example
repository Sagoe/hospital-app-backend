PORT=8000
NODE_ENV=development

# PostgreSQL connection string (asyncpg driver)
DATABASE_URL=postgresql://postgres.iotyjzovbedknlzkgdiy:Homeland%40gh7890@aws-0-eu-west-1.pooler.supabase.com:5432/postgres


# JWT
JWT_SECRET=replace-this-with-a-64-byte-random-hex-string
JWT_ACCESS_EXPIRATION_MINUTES=30

# AES-256-GCM key for field-level encryption at rest.
# Must be a URL-safe base64-encoded 32-byte key. Generate with:
#   python -c "import base64, os; print(base64.urlsafe_b64encode(os.urandom(32)).decode())"
DATA_ENCRYPTION_KEY=replace-this-with-a-32-byte-base64-key

# Twilio (telehealth video rooms)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=replace-with-twilio-auth-token

# Stripe (billing)
STRIPE_SECRET_KEY=sk_test_replace_with_real_key
